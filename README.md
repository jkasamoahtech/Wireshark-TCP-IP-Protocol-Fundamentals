# Wireshark-TCP-IP-Protocol-Fundamentals
I will use wireshark to capture and save packets on a physical wired network, also create a filter to observe TCP/IP Packet, and observe HTTP &amp; HTTPS  TCP/IP Protocol and Finally Identify the TCP/IP Protocol Stack.

